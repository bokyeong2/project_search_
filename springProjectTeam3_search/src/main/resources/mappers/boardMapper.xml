<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- <mapper namespace="com.petpark.mapper.BoardMapper"> -->
<mapper namespace="mapper.BoardMapper">

	<!-- 상품 검색 -->
	<select id="getProductList" resultType="domain.BoardDTO">
	
			select  product_number, productCategory_number, product_name, product_price, product_stock, product_desc, product_date, product_sell
			from product
			<include refid="criteria"></include>
			order by product_number desc 
			limit #{skip}, #{amount}	
	
	</select>
	
	<!-- 상품 총 개수 -->
	<select id="productGetTotal" resultType="int">

  		select count(*) from product
  		
		<include refid="criteria"></include>
		
	</select>
	
	
	<select id="boardList" resultType="domain.BoardDTO">
		select * from board where user_id = #{user_id} and boardCategory_number = 1;
	</select>


	<!-- 게시판 등록 / 나중에 지울거임 -->
	<insert id="insertBoard">
	
		insert into board(board_numberID, boardCategory_number, user_id, board_title, board_contents, board_createDate, board_updateDate, 
						board_deleteDate, board_number, board_hit, board_deleteCheck)
		values(#{board_numberID}, #{boardCategory_number}, #{user_id}, #{board_title}, #{board_contents}, #{board_createDate}, #{board_updateDate}, 
				#{board_deleteDate}, #{board_number}, #{board_hit}, #{board_deleteCheck})
				
	</insert>

	<!-- 게시판 목록 -->
	<select id="getList" resultType="domain.BoardDTO">
	
		select * from board
	
	</select>

	<select id="getMaxNum" resultType="java.lang.Integer">
		select max(num) from board
	</select>
	
	<select id="getBoardList" resultType="domain.BoardDTO">
		select * from board order by board_numberID desc limit #{startRow}, #{pageSize}
	</select>
 
 	<select id="getBoardCount"  resultType="java.lang.Integer">
 		select count(*) from board
 	</select>
 	
</mapper>